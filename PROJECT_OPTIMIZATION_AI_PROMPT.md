# 当前项目优化方案 AI 提示词

你现在是一名资深全栈架构师、桌面端工程师、音视频处理工程师、AI 应用工程师。请基于以下项目背景，对这个项目做一次“全局优化方案设计”，范围不是只看 TTS，而是覆盖整个项目。

你的任务不是直接重写整个项目，而是先输出一份高质量、可执行、能落地的优化方案。请站在“后续要真实改代码”的角度来分析，避免空泛建议。

## 一、项目背景

这是一个本地运行的字幕编辑与处理工具，技术栈如下：

- 前端：Vue 3、Element Plus、Pinia、Vite
- 桌面端：Electron
- 后端：Python、Flask
- AI/音视频能力：
  - Whisper / whisper.cpp / ctranslate2 用于语音识别
  - Spark-TTS / Qwen3-TTS 用于配音与声音克隆
  - FFmpeg 用于音视频处理、波形、硬字幕、配音合成

## 二、项目主要功能

当前项目包含但不限于以下能力：

- 字幕导入、编辑、保存、撤销重做
- SRT / VTT / ASS / SSA 等字幕格式处理
- 视频或音频加载、波形显示、时间轴对齐
- 语音识别（Whisper、whisper.cpp、Vosk 等）
- 字幕翻译（Ollama、DeepSeek、阿里百炼及其他兼容引擎）
- 拼写检查、长句切分、句子合并、重复检测
- TTS 配音（Spark-TTS、Qwen3-TTS）
- 参考音频上传与声音克隆
- 配音音频与视频合成
- 硬字幕嵌入视频
- Electron 桌面端打包与本地文件访问

## 三、已知项目结构

项目目录大致如下：

```text
subtitle_tool/
├── backend/
│   ├── config/
│   ├── routes/
│   ├── services/
│   └── utils/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── stores/
│   │   ├── services/
│   │   └── views/
│   └── electron/
├── Spark-TTS/
├── Qwen3-TTS/
├── Release/
├── Temp/
├── app.py
├── README.md
└── requirements.txt
```

## 四、你需要重点关注的问题

请围绕以下方向做系统级优化分析：

### 1. 架构与模块边界

- 前后端职责是否清晰
- `routes / services / utils` 的边界是否合理
- 是否存在重复逻辑、强耦合、全局状态污染
- Electron、Flask、前端状态管理之间的职责是否清楚
- 是否适合继续单体架构，还是应该做“轻度模块化重构”

### 2. 性能与响应速度

- 前端渲染性能
- 大字幕文件编辑性能
- 波形加载性能
- 翻译批处理效率
- TTS 配音速度
- Whisper 转写速度
- FFmpeg 调用链是否存在不必要的串行阻塞
- 是否存在可批处理、并行化、缓存化、惰性加载的点

### 3. 稳定性与容错

- 长任务状态轮询是否可靠
- 后端子进程管理是否稳健
- 任务取消、失败恢复、重试机制是否完善
- 临时文件清理是否足够安全
- Electron 启动后端、健康检查、异常退出恢复是否健壮
- 音视频处理失败时是否能给用户清晰反馈

### 4. 代码质量与可维护性

- 是否存在“单文件过大、单组件职责过重、单服务过度膨胀”的问题
- 是否缺少统一的错误处理、日志规范、配置管理
- 是否缺少通用任务抽象，例如：
  - 长任务管理器
  - 子进程执行器
  - 文件资源管理器
  - AI 引擎适配层
- 是否需要统一前端 API 调用层、状态层、文件处理层

### 5. 用户体验

- 长时间任务的可视化反馈是否足够
- TTS、转写、翻译、硬字幕生成等重任务是否应该统一交互体验
- 配置项是否易懂
- 默认值是否合理
- 错误提示是否能帮助用户解决问题
- 是否应加入预检机制，例如：
  - 检查 FFmpeg 是否可用
  - 检查模型是否存在
  - 检查 CUDA 是否可用
  - 检查 API Key / Ollama 服务是否可用

### 6. AI 能力接入方式

- 翻译、拼写检查、切分、TTS 等 AI 能力是否应统一成适配器层
- 不同模型 / 引擎的参数管理是否应统一
- 提示词是否应配置化
- 是否应该引入任务级缓存、分段缓存、结果缓存
- 是否应区分“高质量模式”和“极速模式”

### 7. 打包、部署与运行环境

- Electron + Flask 当前联动方式是否合理
- 开发环境、生产环境配置是否清晰
- Python 依赖、模型文件、FFmpeg、whisper.cpp、CUDA 的依赖管理是否易出问题
- Windows 本地桌面场景下，是否需要更稳妥的路径管理、权限处理、日志落盘方案

### 8. 测试与工程化

- 当前项目可能缺少哪些测试：
  - 单元测试
  - 服务层测试
  - API 测试
  - Electron 集成测试
  - 关键流程回归测试
- 是否需要加入 lint / typecheck / smoke test / 构建前检查
- 是否需要更清晰的开发脚本、诊断脚本、环境检查脚本

## 五、结合当前项目现状的额外上下文

请把以下现状纳入你的分析：

- 项目是本地桌面应用，不是纯 Web SaaS
- 项目有较多长耗时任务：转写、翻译、TTS、硬字幕、音视频合成
- 部分功能依赖 Python 子进程、模型推理和 FFmpeg
- TTS 目前至少有两条链路：Spark-TTS 与 Qwen3-TTS
- Qwen3-TTS 存在批量生成思路，Spark-TTS 仍可能存在逐条串行推理问题
- 前端使用轮询方式获取长任务状态
- 项目已经具备一定功能广度，但整体工程化程度还有继续提升空间

## 六、输出要求

请严格按以下结构输出，不要省略：

### 1. 项目总体判断

先用 1 到 3 段话总结：

- 当前项目的优点
- 当前项目最突出的结构性问题
- 这个项目下一阶段最值得投入的优化方向

### 2. 按模块拆解的优化建议

请至少覆盖以下模块：

- 前端界面层
- 前端状态管理层
- 前端 API / 任务交互层
- Electron 主进程与 preload
- Flask 路由层
- Python 服务层
- TTS 模块
- 转写模块
- 翻译模块
- 音视频处理与 FFmpeg 调用层
- 配置与环境管理
- 日志、错误处理、临时文件管理

每个模块请输出以下内容：

- 当前可能存在的问题
- 优化目标
- 建议的改造方案
- 改造优先级：P0 / P1 / P2
- 改造成本：低 / 中 / 高
- 预期收益

### 3. 必做优化清单

请输出一个分级清单：

- P0：必须优先做，否则会持续拖累开发与体验
- P1：做完后整体体验明显提升
- P2：中长期优化项

每项要写清楚：

- 具体要做什么
- 为什么要做
- 会影响哪些模块
- 是否适合拆分为多个 PR

### 4. 技术方案建议

请给出你建议引入或统一的工程能力，例如：

- 统一任务执行框架
- 统一 AI 引擎适配层
- 统一配置中心
- 统一日志与诊断体系
- 统一子进程执行器
- 统一缓存机制
- 统一错误码与用户提示体系

### 5. 风险与注意事项

请特别指出：

- 哪些改动最容易引入回归
- 哪些模块最适合先重构
- 哪些模块不要一次改太大
- 哪些优化要优先加测试后再动

### 6. 建议的实施路线图

请给出一个现实可行的路线图，例如：

- 第 1 周做什么
- 第 2 周做什么
- 第 3 周做什么
- 第 4 周做什么

要求遵循“小步快跑、可持续交付、尽量不打断现有功能”的原则。

## 七、输出风格要求

- 不要只讲大而空的最佳实践
- 必须结合这个项目“本地桌面应用 + Flask + Electron + AI 音视频任务”的特点
- 优先给能落地的建议
- 允许提出重构，但不要默认建议推翻重写
- 你的建议必须体现“先稳住主干流程，再逐步做架构优化”的思路

## 八、额外要求

请在最后补充两部分内容：

### A. 如果只能先做 5 件事，你会选哪 5 件

请给出最有性价比的前 5 项优化。

### B. 如果要让 AI 直接开始改代码，第一批最适合动手的文件有哪些

请结合这个项目，优先列出最适合首批改造的文件或目录，例如：

- `backend/services/...`
- `backend/routes/...`
- `frontend/src/components/...`
- `frontend/src/stores/...`
- `frontend/electron/...`

并说明原因。

---

如果你觉得有必要，可以在分析过程中先自行假设该项目目前存在以下常见问题，并在方案里明确验证：

- 长任务实现分散
- 状态管理粒度不一致
- 部分服务层逻辑过于集中
- 子进程调用缺少统一封装
- 配置项分散在前后端
- UI 上缺少统一任务反馈与错误恢复机制
- 对模型、FFmpeg、CUDA、路径依赖的预检查不完善

请开始输出完整优化方案。
