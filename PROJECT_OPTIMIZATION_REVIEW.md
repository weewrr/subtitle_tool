# 字幕工具项目评审与优化建议

## 总体看法

这是一个功能覆盖面很广的桌面字幕工具：除视频/音频转写外，还包含字幕编辑、翻译、拼写检查、波形、硬字幕和 TTS。技术路线务实，Flask 后端与 Vue + Electron 前端的组合也适合本地 AI 推理场景。

项目当前最有价值的优势是“本地工作流完整”：用户能在一个应用内完成从媒体文件到字幕、编辑、翻译和导出的闭环。同时接入 OpenAI Whisper、Whisper.cpp、faster-whisper 三种识别实现，给不同硬件条件留下了选择空间。

目前的主要挑战不在单个功能，而在工程化边界：长时间 AI 任务、多个处理引擎、模型和临时文件都已出现，需要从“单次操作脚本”逐步演进为“可观测、可恢复的任务系统”。

## 已改善的部分

转录流程已具备以下基础能力：

- 以 `task_id` 隔离任务状态，避免批处理与单文件任务互相覆盖。
- 使用 SSE 推送进度，并在前端降级为轮询。
- ffmpeg、OpenAI Whisper、Whisper.cpp 和 faster-whisper 都接入了可用的进度来源。
- 支持任务取消和失败状态回传。
- Electron 等待后端健康检查成功后再展示窗口，解决启动顺序问题。

这些改造使语音识别从“黑盒等待”变为可反馈的后台任务，也为后续统一其他耗时功能提供了模板。

## 高优先级建议

### 1. 统一所有耗时操作的任务框架

目前硬字幕、TTS、模型下载、波形生成和转录各自维护状态与接口，字段、取消行为和进度机制不一致。建议提炼通用 `TaskManager`，统一处理：

- `task_id`、任务类型、创建时间、状态、阶段、进度、错误和结果。
- SSE 状态流、查询、取消和结果读取接口。
- 任务事件日志与保留期清理。
- CPU/GPU 队列和并发限制。

建议统一 API 形态：

```text
POST   /api/tasks                     创建任务
GET    /api/tasks/{task_id}           查询状态
GET    /api/tasks/{task_id}/events    SSE 事件流
POST   /api/tasks/{task_id}/cancel    请求取消
GET    /api/tasks/{task_id}/result    读取结果
```

这样前端只需维护一个通用任务进度组件，后续新增 AI 功能不会重复实现轮询和异常处理。

### 2. 明确 GPU 资源调度

Whisper、faster-whisper、TTS 和硬字幕可能争用 GPU 显存。当前若多个任务同时运行，容易发生显存不足、驱动错误或明显变慢。

建议：

- 将 GPU 任务默认串行化，CPU 任务使用有限线程池。
- 任务开始前记录显存可用量、模型和推理精度。
- 遇到 CUDA 资源不足时，提供“等待 GPU”或“自动回退 CPU”的明确选项。
- 模型缓存使用 LRU 策略，并在空闲时按需释放，避免多模型长期占用显存。

### 3. 补齐任务可靠性与恢复能力

当前任务状态主要在内存中。应用崩溃、后端重启或 Electron 重载后，任务和结果会丢失。

建议先使用 SQLite（不需要部署独立数据库）持久化：

- 任务元数据、进度快照、错误堆栈和结果文件路径。
- 应用启动时将未完成任务标为“已中断”，可重新执行。
- 为长任务提供“重新打开后查看结果”能力。

## 中优先级建议

### 4. 梳理转录引擎定位与默认参数

三种 Whisper 引擎应在 UI 中清楚标明适用条件：

| 引擎 | 推荐场景 | 进度能力 | 注意事项 |
| --- | --- | --- | --- |
| faster-whisper | 默认选择，速度优先 | 分块真实进度 | 推荐 GPU，适合大多数用户 |
| OpenAI Whisper | 兼容与基准对比 | 帧级真实进度 | 较慢、显存和依赖更敏感 |
| Whisper.cpp | 轻量、CPU/低配设备 | CLI 进度 | 模型格式与 CLI 版本需要匹配 |

此外可提供“速度优先 / 平衡 / 质量优先”预设，而不是让普通用户直接理解 `beam_size`、计算精度和模型差异。

### 5. 减少运行时安装依赖

服务请求期间自动执行 `pip install` 会带来网络、权限、环境污染和进度不可控问题。依赖应在安装包构建或启动前检查阶段完成。

建议：

- 在设置页展示 Python、ffmpeg、CUDA、模型路径和依赖自检结果。
- 缺失依赖时给出一键安装或明确的修复说明。
- 运行中的任务只执行业务逻辑，不修改 Python 环境。

### 6. 优化临时文件与磁盘治理

视频转写、模型下载、波形和 TTS 会产生较大临时文件。建议建立统一的临时文件管理器：

- 每个任务使用独立目录，例如 `temp/tasks/{task_id}`。
- 正常完成、失败和取消时走统一清理逻辑。
- 延迟清理最近任务，便于用户导出日志或恢复结果。
- 启动时清理过期目录，并在磁盘空间不足前提示用户。

## 前端与体验建议

### 7. 增加全局任务中心

目前长任务大多依附于弹窗。建议增加状态栏入口和任务中心：

- 后台任务不因关闭弹窗而丢失可见性。
- 显示阶段、进度、耗时、预计剩余时间、取消和重试。
- 识别完成后提供“打开字幕”“定位结果文件”等操作。

### 8. 统一失败提示与诊断信息

用户只看到类似 `Errno 22` 的底层错误时，很难自行处理。建议错误响应包含：

```json
{
  "code": "TRANSCRIBE_INPUT_FAILED",
  "message": "音频标准化失败，请检查文件是否可播放",
  "detail": "供日志诊断使用的原始异常",
  "recoverable": true
}
```

前端默认展示 `message`，并提供“复制诊断信息”按钮，避免直接暴露无法理解的异常文本。

### 9. 提升可访问性与视觉一致性

- 统一所有进度条的文字颜色、成功/失败颜色和阶段文案。
- 任务进行中允许用户最小化或切换页面，而不是只能停留在模态框。
- 为模型、GPU、磁盘空间和输出文件提供更明确的状态反馈。
- 修复历史文件中的乱码编码；统一以 UTF-8 保存源代码和文案。

## 代码质量与维护性建议

### 10. 处理编码与异常处理一致性

部分 Python/Vue 源文件已有乱码注释或文案，说明历史编码不一致。建议一次性统一为 UTF-8，并在 `.editorconfig`、Git 属性和编辑器配置中固定编码与换行规则。

异常处理也应统一：避免裸 `except:`，至少记录异常类型与堆栈；同时不要吞掉清理失败以外的业务错误。

### 11. 加入基础自动化测试

优先测试最容易回归的边界：

- 上传后返回唯一 `task_id`。
- SSE 状态顺序与终态正确。
- 取消任务不再产生完成结果。
- ffmpeg 失败、模型不存在、GPU 不可用时能返回可读错误。
- SRT 解析、时间戳转换和分段合并正确。

前端至少增加 API Mock 测试，覆盖连接断开、任务失败、结果为空和重试场景。

### 12. 建立结构化日志

建议所有任务日志带上 `task_id`、引擎、模型、设备、媒体时长和耗时：

```text
task_id=... engine=openai model=base device=cuda phase=transcribing elapsed=41.2s
```

这会显著缩短性能和偶发故障的定位时间。

## 推荐实施路线

### 第一阶段：稳定性（近期）

1. 通用任务状态模型覆盖 TTS、硬字幕和波形。
2. 统一错误码、任务日志和临时目录。
3. 固定 UTF-8 编码，清理乱码文本。
4. 增加后端健康检查、依赖自检和启动诊断。

### 第二阶段：性能与体验（中期）

1. GPU 队列、模型缓存和显存回收策略。
2. SQLite 持久化任务与结果。
3. 全局任务中心和后台任务恢复。
4. 模型/质量预设与硬件推荐。

### 第三阶段：可维护性（长期）

1. 自动化测试与持续构建。
2. 可配置的任务执行器和插件化识别引擎。
3. 性能指标采集：实时系数、模型加载耗时、错误率和硬件信息。
4. 安装包中的 Python、ffmpeg、模型和依赖版本可复现化。

## 总结

项目的产品方向是清晰的：它不是单一转写工具，而是面向本地创作者的字幕生产工作台。现阶段最值得投入的是把已有功能统一到可靠的后台任务与资源管理体系中。这样既能解决长任务体验、偶发错误和启动顺序问题，也能让翻译、TTS、硬字幕等已有能力自然扩展，而不需要为每个功能重复维护一套进度、状态和错误处理逻辑。
